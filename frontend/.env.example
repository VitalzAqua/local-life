# Copy to .env and fill in real values. Never commit .env.

# Optional. Leave unset if the frontend and backend are deployed on the same origin.
# Set this when the API is hosted on a different domain.
REACT_APP_API_URL=http://localhost:3001
